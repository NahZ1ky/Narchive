#include <stdio.h>
#include <string.h>

int main(void) {
    // variable declaration
    char* humanName;
    char* dogName;
    char* rabbitName;
    int humanAge;

    printf("Please enter your name: ");
    // humanName = getchar(); // need more work
    printf("%s", humanName);
}